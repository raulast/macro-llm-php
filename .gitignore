vendor
.kiro
.atl
.env
.idea
composer.lock
openspec
.phpunit.cache
.phpunit.result.cache
# Machine-local SDD preflight state (mirrors the openspec/ decision)
.pi
# Local ODD task tracking (same local-only decision as openspec/ and .pi)
odd
